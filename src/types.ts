import type { ProseRecipeOptions } from "./prose/types";

/**
 * Options for the typography preset.
 */
export interface TypographyPresetOptions {
  /**
   * Options for the prose recipe.
   */
  prose?: ProseRecipeOptions;
}
